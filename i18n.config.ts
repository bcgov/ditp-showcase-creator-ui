//const path = require('path');

const i18nConfig = {
  // i18n: {
    locales: ['en', 'fr'],
    defaultLocale: 'en',
    //localePath: path.resolve('./locales'),
  // },
};

export default i18nConfig;
