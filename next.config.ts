import type { NextConfig } from "next";
//import i18nConfig from './i18nConfig';

// TODO move these to supported lang types
const nextConfig: NextConfig = {
  // i18n: i18nConfig
};

export default nextConfig;
